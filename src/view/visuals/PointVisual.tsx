import ReactDOM from "react-dom";

class PointVisual {
  private _text: JSX.Element;

  constructor() {
    this._text = <div>Test</div>;
    ReactDOM.render(test, document.body);
  }

  render() {
    //this._text.props.
  }
}

export default PointVisual;
